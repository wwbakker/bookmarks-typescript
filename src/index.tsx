import * as React from "react";
import * as ReactDOM from "react-dom";

import {BookmarkNavigator} from "./components/Hello";

ReactDOM.render(
    <BookmarkNavigator />,
    document.getElementById("example")
);
